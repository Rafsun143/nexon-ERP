import {getCollection,saveCollection} from "./storage.js";import {openModal,closeModal} from "./modal.js";import {toast} from "./toast.js";export function init(){render()}function render(){const p=getCollection("products");document.getElementById("pageContent").innerHTML=`<div class="page-head"><div><h2>Inventory</h2><p>Monitor stock, stock-in and stock-out movements.</p></div><button class="btn btn-primary" id="move">＋ Stock Movement</button></div><div class="grid grid-3">${p.map(x=>`<div class="card"><div class="card-body"><div style="display:flex;justify-content:space-between;gap:10px"><div><strong>${x.name}</strong><div class="muted" style="font-size:10px;margin-top:4px">${x.category}</div></div><span class="badge ${x.status==="In Stock"?"badge-success":x.status==="Low Stock"?"badge-warning":"badge-danger"}">${x.status}</span></div><div class="stat-value">${x.quantity}</div><div class="muted" style="font-size:10px">units available • reorder at ${x.reorder}</div><div class="progress" style="margin-top:12px"><span style="width:${Math.min(100,x.quantity/Math.max(x.reorder*3,1)*100)}%"></span></div></div></div>`).join("")}</div>`;document.getElementById("move").onclick=()=>movement()}
function movement(){const p=getCollection("products");openModal({title:"Stock Movement",body:`<form id="f" class="form-grid"><div class="field full"><label>Product</label><select name="id">${p.map(x=>`<option value="${x.id}">${x.name} — ${x.quantity} units</option>`).join("")}</select></div><div class="field"><label>Movement</label><select name="type"><option value="in">Stock In</option><option value="out">Stock Out</option></select></div><div class="field"><label>Quantity</label><input name="qty" type="number" min="1" value="1"></div></form>`,footer:`<button class="btn btn-light" data-close>Cancel</button><button class="btn btn-primary" id="save">Apply Movement</button>`});document.getElementById("save").onclick=()=>{const d=Object.fromEntries(new FormData(document.getElementById("f")));const q=Math.max(1,Number(d.qty));const a=p.map(x=>{if(x.id!==Number(d.id))return x;let n=d.type==="in"?x.quantity+q:x.quantity-q;if(n<0){toast("Insufficient stock.","error");return x}return {...x,quantity:n,status:n<=0?"Out of Stock":n<=x.reorder?"Low Stock":"In Stock"}});saveCollection("products",a);closeModal();toast("Inventory updated.");render()}}